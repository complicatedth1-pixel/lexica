Lexica — Split File Reference

How to use this doc:
Each ## FILE: filename.ext section below is one file. Copy the code block under it and save as that filename. All files go in the same folder. Open index.html in browser.


Project Tree
lexica/
├── index.html          ← HTML skeleton + script/link tags only
├── style-base.css      ← Reset, :root vars, homepage, modals, book cards, footer
├── style-editor.css    ← Topbar, sidebar, page-card, sections, analytics, highlights, PDF, toast
├── auth.js             ← Supabase client, login/logout, Google OAuth, user menu
├── library.js          ← window.library state, saveBook, renderHomepage, book modals, PDF import
├── editor.js           ← Tree render, section CRUD, toolbar, autosave, word count
├── highlights.js       ← applyPreciseHighlight, sidebar HL cards, Highlights page
├── stopwatch.js        ← Timer state, save time to topic, PDF page time
├── analytics.js        ← openAnalytics, renderAnalytics, WPM calc
└── pdf.js              ← PDF.js viewer, canvas render, outline sidebar, PDF highlights

Quick Bug-Location Guide
SymptomFile to sendLogin / sign-out brokenauth.jsBooks not saving / loading from cloudlibrary.jsHomepage grid or last-book card wronglibrary.jsSidebar tree wrong / drag brokeneditor.jsSections modal / section contenteditor.jsToolbar bold / italic / heading brokeneditor.jsHighlight not applying or wrong colorhighlights.jsHighlights page empty or wronghighlights.jsStopwatch not saving time to topicstopwatch.jsAnalytics WPM wrong / not updatinganalytics.jsPDF not loading or renderingpdf.jsPDF page highlights lostpdf.jsCSS broken on homepagestyle-base.cssCSS broken in editorstyle-editor.cssModal z-index / overlapstyle-base.css or style-editor.css

Shared Globals
VariableOwnerRead bywindow.librarylibrary.jsallwindow.activeBookIdlibrary.jseditor, analyticstreeDataeditor.jsstopwatch, highlightsselectedChapterIdeditor.jsstopwatch, highlightsselectedTopicIdeditor.jsstopwatch, highlightspdfModepdf.jshighlights, stopwatchcurrentUserauth.jslibrary_savedRangeeditor.jshighlights (mobile sel)

Script Load Order (bottom of index.html <body>)
html<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="auth.js"></script>
<script src="library.js"></script>
<script src="stopwatch.js"></script>
<script src="highlights.js"></script>
<script src="analytics.js"></script>
<script src="pdf.js"></script>
<script src="editor.js"></script>

database-
-- Books/library metadata table
create table public.books (
  id text not null,
  user_id uuid not null references auth.users(id) on delete cascade,
  name text not null default '',
  tree_data jsonb not null default '[]',
  highlights jsonb not null default '{}',
  notes jsonb not null default '{}',
  last_opened bigint not null default 0,
  is_pdf boolean not null default false,
  is_pdf_viewer boolean not null default false,
  pdf_num_pages integer,
  page_times jsonb not null default '{}',
  pdf_highlights jsonb not null default '{}',
  created_at timestamptz not null default now(),
  primary key (id, user_id)
);

-- RLS
alter table public.books enable row level security;
create policy "Users see own books" on public.books for all using (auth.uid() = user_id);

-- Storage bucket for PDFs
insert into storage.buckets (id, name, public) values ('pdfs', 'pdfs', false);

-- Storage RLS: users can only access their own folder
create policy "User pdf upload" on storage.objects for insert
  with check (bucket_id = 'pdfs' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "User pdf read" on storage.objects for select
  using (bucket_id = 'pdfs' and (storage.foldername(name))[1] = auth.uid()::text);

create policy "User pdf delete" on storage.objects for delete
  using (bucket_id = 'pdfs' and (storage.foldername(name))[1] = auth.uid()::text);
